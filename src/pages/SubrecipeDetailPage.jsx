// Sub-recipes share the same form as recipes.
// The isSubRecipe flag and yield fields are shown when ?type=subrecipe is in the URL
// or when the loaded recipe has isSubRecipe === true.
export { default } from './RecipeDetailPage'
